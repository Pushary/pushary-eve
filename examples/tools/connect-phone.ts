// Eve discovers tools by file. This exposes a `connect-phone` tool that returns the
// one-tap link an end-user opens to turn on approvals.
import { pusharyConnectPhone } from '@pushary/eve'

export default pusharyConnectPhone()
